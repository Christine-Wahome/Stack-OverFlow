import { NoOfanswersPipe } from './no-ofanswers.pipe';

describe('NoOfanswersPipe', () => {
  it('create an instance', () => {
    const pipe = new NoOfanswersPipe();
    expect(pipe).toBeTruthy();
  });
});
