import { UnansweredPipe } from './unanswered.pipe';

describe('UnansweredPipe', () => {
  it('create an instance', () => {
    const pipe = new UnansweredPipe();
    expect(pipe).toBeTruthy();
  });
});
